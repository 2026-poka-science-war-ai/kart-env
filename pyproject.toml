[project]
name = "kart-env"
version = "0.1.0"
description = "Kart environment for reinforcement learning"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pettingzoo==1.25.0",
    "pillow",
]
