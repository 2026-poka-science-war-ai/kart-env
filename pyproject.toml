[project]
name = "kart-env"
version = "0.1.0"
description = "Kart environment based on PettingZoo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pettingzoo==1.25.0",
    "pillow",
]
